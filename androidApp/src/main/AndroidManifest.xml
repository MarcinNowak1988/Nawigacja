<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Wyznaczanie trasy wymaga sieci (ADR-0007). Bez tego uprawnienia brak zasięgu
         objawiałby się dopiero błędem połączenia, zamiast czytelnym komunikatem. -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Aplikacja działa bez tych czujników, tylko z ograniczoną funkcjonalnością:
         bez GPS nie ma pozycji ani prędkości, bez barometru nie ma wykrywania burzy. -->
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />
    <uses-feature android:name="android.hardware.sensor.barometer" android:required="false" />
    <uses-feature android:name="android.hardware.sensor.accelerometer" android:required="false" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.DeviceDefault.Light">

        <!-- adjustResize zamiast przesuwania okna: pulpit jest w ScrollView, więc po
             zmniejszeniu okna sam przewinie się do pola, w którym stoi kursor. Przy
             domyślnym zachowaniu klawiatura zasłaniała wpisywany tekst. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
